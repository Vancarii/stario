/// The artist of an song
class ArtistModel {
  /// Name of the artist.
  final String artistName;

  /// An Uri to an image representing this artist
  final Uri profileImage;

  ArtistModel(this.artistName, this.profileImage);
}
